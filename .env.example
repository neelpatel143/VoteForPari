
# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="https://vote-for-pari-git-main-neelpatel143s-projects.vercel.app/"

# MongoDB connection string for the support counter database
MONGODB_URI="mongodb+srv://Neel15:MongoDBPassword@clusterjmc.f8veynq.mongodb.net/?appName=ClusterJMC"

# Secret password to reset the support counter (e.g. POST /api/votes/reset)
RESET_SECRET="votededoaurcoolbano"
